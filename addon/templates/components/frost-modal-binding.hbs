{{#if isVisible}}
  {{to-elsewhere
    named=targetOutlet
    send=(hash
      animation=animation
      body=(component modalComponentName
        content=content
        params=params
        onCancel=(action '_onCancel')
        onClose=onClose
        onConfirm=(action '_onConfirm')
      )
      classModifier=classModifier
      onOutsideClick=(action '_onOutsideClick')
    )
  }}
{{/if}}
